# Legacy Snapshot

This `_legacy` directory is an archival snapshot of a previous implementation.

- It is not part of the active runtime for the current `client` or `server`.
- It is kept only for historical reference during migration/rebuild work.
- Automated tests in the active codebase do not execute legacy modules.

